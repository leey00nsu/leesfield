diff --git a/src/components/splitgrid/TemplateNodes.tsx b/src/components/splitgrid/TemplateNodes.tsx
--- a/src/components/splitgrid/TemplateNodes.tsx
+++ b/src/components/splitgrid/TemplateNodes.tsx
@@ -23,6 +23,7 @@
 } from "@xyflow/react";
 import {
   GEMINI_IMAGE_MODELS,
+  useWorkflowStore,
 } from "../../leesfield/upstream-node-host";
 import type {
   AspectRatio,
@@ -299,7 +300,8 @@
   }, [isBrowseDialogOpen]);
 
   const selectedModel = overrides.selectedModel as SelectedModel | undefined;
-  const currentProvider = selectedModel?.provider ?? "leesfield";
+  const catalog = useWorkflowStore((state) => state.hostedModels) ?? [];
+  const currentProvider = catalog.find((model) => model.key === selectedModel?.modelId)?.provider ?? selectedModel?.provider ?? "hf_space";
   // Same shape as GenerateImageNode.handleBrowseModelSelect
   const handleBrowseModelSelect = useCallback(
     (model: ProviderModel) => {
diff --git a/src/components/splitgrid/SplitGridTemplateModal.tsx b/src/components/splitgrid/SplitGridTemplateModal.tsx
--- a/src/components/splitgrid/SplitGridTemplateModal.tsx
+++ b/src/components/splitgrid/SplitGridTemplateModal.tsx
@@ -750,7 +750,7 @@
         backdropPointerDownRef.current = false;
       }}
     >
-      <div className="relative w-[min(1080px,94vw)] h-[min(720px,88vh)] bg-neutral-800 rounded-xl border border-neutral-700 shadow-2xl overflow-clip flex flex-col">
+      <div role="dialog" aria-modal="true" aria-label="Cell template" className="relative w-[min(1080px,94vw)] h-[min(720px,88vh)] bg-neutral-800 rounded-xl border border-neutral-700 shadow-2xl overflow-clip flex flex-col">
         {/* Header */}
         <div className="flex max-sm:flex-wrap items-center justify-between gap-4 px-5 py-3.5 border-b border-neutral-700/60 shrink-0">
           <div className="min-w-0">
diff --git a/src/components/nodes/SplitGridNode.tsx b/src/components/nodes/SplitGridNode.tsx
--- a/src/components/nodes/SplitGridNode.tsx
+++ b/src/components/nodes/SplitGridNode.tsx
@@ -297,8 +297,7 @@
             <GridDimField label="Columns" value={gridCols} onChange={handleColsChange} disabled={isRunning} />
           </div>
 
-          {/* Cell template editing is intentionally unavailable in the hosted
-              graph because its downstream materialization cannot be persisted. */}
+          {/* Cell templates are committed atomically through the hosted graph adapter. */}
           {!templateEditingDisabled && <button
             onClick={() => setShowEditor(true)}
             disabled={isRunning}
